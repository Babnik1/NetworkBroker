# Network Broker

Небольшой асинхронный брокер сообщений на **C++17**, обеспечивающий обмен сообщениями между несколькими клиентами по сети.

Проект реализован с использованием **Boost.Asio** и построен по принципу брокера сообщений: клиенты подключаются к серверу, регистрируются или авторизуются, создают тематические каналы, подписываются на них и публикуют сообщения.

Брокер самостоятельно определяет, каким подписанным клиентам необходимо доставить опубликованное сообщение.

---

## Возможности

* асинхронная обработка TCP-соединений;
* одновременная работа с несколькими клиентами;
* регистрация и авторизация клиентов;
* создание тематических каналов;
* подписка и отписка клиентов от каналов;
* публикация сообщений;
* маршрутизация сообщений подписчикам;
* хранение клиентов и топиков в JSON;
* многопоточная обработка журналирования;
* конфигурация приложения через JSON;
* логирование событий приложения;
* модульные тесты;
* интеграционные тесты;
* автоматическая сборка и тестирование через GitHub Actions;
* автоматическая генерация документации Doxygen.

---

## Архитектура

Проект разделён на несколько основных компонентов:

```text
                         ┌─────────────────┐
                         │     Client      │
                         └────────┬────────┘
                                  │ TCP
                                  ▼
                         ┌─────────────────┐
                         │     Server      │
                         │   Boost.Asio    │
                         └────────┬────────┘
                                  │
                                  ▼
                         ┌─────────────────┐
                         │     Session     │
                         └────────┬────────┘
                                  │
                                  ▼
                         ┌─────────────────┐
                         │ MessageBroker   │
                         └───────┬─┬───────┘
                                 │ │
                    ┌────────────┘ └────────────┐
                    ▼                           ▼
             ┌───────────────┐           ┌───────────────┐
             │ ClientManager │           │ TopicManager  │
             └───────┬───────┘           └───────┬───────┘
                     │                           │
                     ▼                           ▼
             ┌───────────────┐           ┌───────────────┐
             │ Client JSON DB│           │ Topic JSON DB │
             └───────────────┘           └───────────────┘
```

### Основные компоненты

**Server**

Отвечает за TCP-соединения и принимает новых клиентов с помощью Boost.Asio.

**Session**

Представляет отдельное сетевое соединение с клиентом и взаимодействует с брокером.

**MessageBroker**

Центральный компонент приложения. Разбирает команды клиента и передаёт их соответствующим менеджерам.

**ClientManager**

Управляет зарегистрированными клиентами, их подключением и соответствием между `SessionId` и `ClientId`.

**TopicManager**

Управляет тематическими каналами и списками их подписчиков.

**JSON repositories**

Отвечают за сохранение и загрузку данных клиентов и топиков.

**Logger**

Выполняет асинхронное журналирование через потокобезопасную очередь. Запись сообщений в файл выполняется отдельным потоком.

---

## Протокол

Клиент взаимодействует с брокером посредством простого текстового протокола.

### Регистрация

```text
REGISTER <name>
```

Пример:

```text
REGISTER alex
```

Ответ:

```text
OK
```

### Авторизация

```text
LOGIN <name>
```

Пример:

```text
LOGIN alex
```

### Создание топика

```text
CREATE <topic>
```

Пример:

```text
CREATE news
```

### Подписка

```text
SUBSCRIBE <topic>
```

Пример:

```text
SUBSCRIBE news
```

### Отписка

```text
UNSUBSCRIBE <topic>
```

### Публикация

```text
PUBLISH <topic> <message>
```

Пример:

```text
PUBLISH news Hello world
```

Сообщение будет отправлено клиентам, подписанным на указанный топик.

---

## Ответы сервера

При успешном выполнении команды сервер возвращает:

```text
OK
```

При ошибке:

```text
ERROR <description>
```

Например:

```text
ERROR Client not found
ERROR Topic not found
ERROR Already subscribed
ERROR You are unauthorized
ERROR Invalid command
```

---

## Хранение данных

Для хранения состояния брокера используется JSON.

### Клиенты

Файл `clients.json`:

```json
{
    "1": {
        "name": "alex"
    },
    "2": {
        "name": "bob"
    }
}
```

### Топики

Файл `topics.json`:

```json
{
    "news": {
        "clients": [
            1,
            2
        ]
    }
}
```

При запуске менеджеры загружают данные из соответствующих JSON-файлов.

---

## Требования

Для сборки необходимы:

* C++17-compatible compiler;
* CMake 3.12 или выше;
* Boost;
* Boost.Asio;
* Boost.Program_options;
* nlohmann/json;
* GoogleTest.

На Ubuntu зависимости можно установить командой:

```bash
sudo apt update

sudo apt install -y \
    build-essential \
    cmake \
    libboost-all-dev \
    nlohmann-json3-dev \
    libgtest-dev \
    doxygen
```

---

## Сборка

Клонирование репозитория:

```bash
git clone <repository-url>
cd NetworkBroker
```

Создание директории сборки:

```bash
cmake -B build -S .
```

Сборка проекта:

```bash
cmake --build build
```

После сборки будут доступны:

```text
Broker
BrokerClient
```

---

## Запуск

### Сервер

```bash
./Broker
```

### Клиент

```bash
./BrokerClient
```

После подключения клиент может выполнять команды протокола:

```text
REGISTER alex
LOGIN alex
CREATE news
SUBSCRIBE news
PUBLISH news Hello world
```

---

## Тестирование

Проект содержит модульные и интеграционные тесты на базе GoogleTest.

Запуск всех тестов:

```bash
ctest --test-dir build --output-on-failure
```

Либо напрямую:

```bash
./build/tests/BrokerTests
```

### Покрываемые компоненты

Тестами проверяются:

* `Client`;
* `ClientManager`;
* `TopicManager`;
* `JsonRepository`;
* `JsonTopicRepository`;
* `MessageBroker`;
* интеграционные сценарии работы брокера.

Интеграционные тесты проверяют реальные взаимодействия между брокером, менеджерами и JSON-хранилищами, в том числе сценарии:

* регистрация клиента;
* создание топика;
* подписка;
* отписка;
* публикация;
* работа нескольких клиентов;
* полный сценарий работы брокера.

Для интеграционных тестов используются отдельные временные JSON-файлы, которые удаляются после выполнения тестов.

---

## Логирование

В проекте реализована собственная система логирования.

Логирование выполняется через потокобезопасную очередь:

```text
Application threads
        │
        ▼
 ┌──────────────┐
 │ Message queue│
 └──────┬───────┘
        │
        ▼
 ┌──────────────┐
 │ Logger thread│
 └──────┬───────┘
        │
        ▼
     app.log
```

Основной поток приложения не выполняет непосредственно запись каждого сообщения в файл. Логирование передаётся отдельному потоку, который последовательно обрабатывает очередь сообщений.

---

## Конфигурация

Параметры приложения задаются через конфигурационный JSON-файл.

Пример:

```json
{
    "log_file_name": "app.log",
    "log_level": "debug",
    "clients_database": "clients.json",
    "topics_database": "topics.json",
    "server_port": 2000
}
```

Конкретные параметры зависят от текущей конфигурации проекта.

---

## Документация

Исходный код документирован с использованием **Doxygen**.

Для локальной генерации документации:

```bash
doxygen Doxyfile
```

HTML-документация будет находиться в:

```text
doc/html/
```

Актуальная документация также публикуется автоматически в GitHub Pages.

---

## CI/CD

Для проекта настроен GitHub Actions.

При `push` и `pull request` автоматически выполняются:

1. установка зависимостей;
2. генерация Doxygen-документации;
3. конфигурация CMake;
4. сборка проекта;
5. запуск тестов.

При создании Git-тега вида:

```text
v1.0.0
```

дополнительно создаётся GitHub Release с исполняемыми файлами:

```text
Broker
BrokerClient
```

Документация автоматически публикуется в ветку `gh-pages`.

---

## Структура проекта

```text
.
├── client/                 # Консольный клиент
│   ├── CMakeLists.txt
│   └── main.cpp
│
├── src/
│   ├── broker/             # Брокер сообщений
│   ├── clients/            # Клиенты и ClientManager
│   ├── configurator/       # Конфигурация
│   ├── db/                 # JSON repositories
│   ├── logs/               # Система логирования
│   ├── server/             # TCP server и Session
│   └── topics/             # TopicManager
│
├── tests/
│   ├── broker/
│   ├── clients/
│   ├── db/
│   ├── integration/
│   ├── mocks/
│   ├── server/
│   └── topics/
│
├── CMakeLists.txt
├── Doxyfile
├── config.json
├── clients.json
└── topics.json
```

---

## Используемые технологии

| Технология                | Назначение                        |
| ------------------------- | --------------------------------- |
| **C++17**                 | Основной язык                     |
| **Boost.Asio**            | Асинхронная TCP-сеть              |
| **Boost.Program_options** | Работа с параметрами конфигурации |
| **nlohmann/json**         | JSON-хранилище                    |
| **GoogleTest**            | Модульные и интеграционные тесты  |
| **CMake**                 | Система сборки                    |
| **Doxygen**               | Генерация документации            |
| **GitHub Actions**        | CI/CD                             |

---

## Цели проекта

Проект предназначен для практического изучения:

* сетевого программирования на C++;
* Boost.Asio;
* асинхронной модели обработки соединений;
* взаимодействия нескольких клиентов;
* архитектуры клиент-серверных приложений;
* многопоточности;
* синхронизации потоков;
* потокобезопасных очередей;
* тестирования C++ приложений;
* работы с CMake;
* автоматизации сборки и тестирования через CI.